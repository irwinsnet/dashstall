[project]
name = "dashstall"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Stacy Irwin", email = "stacy.k.irwin@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "dash>=4.1.0",
]

[project.scripts]
drun = "dashstall.app:main"

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pyinstaller>=6.19.0",
]
